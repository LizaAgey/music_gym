import React from "react";
import {Divider} from "antd";

export const IntervalSettings: React.FC = () => {

    return (
        <>
            <Divider plain>Interval settings</Divider>

            <Divider/>
        </>
    );
}